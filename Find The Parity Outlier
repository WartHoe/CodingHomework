function findOutlier(integers){
  A = integers
  chet = []
  nechet = []
  for (let i = 0; i<A.length; i++){
    if (A[i] % 2 == 0){
      chet.push(A[i])
    } else {
      nechet.push(A[i])
    }
    
  }
  if (chet.length === 1){
    return chet[0]
  } else {
    return nechet[0]
  }
}
